import { StructDirective } from './struct.directive';

describe('StructDirective', () => {
  it('should create an instance', () => {
    const directive = new StructDirective();
    expect(directive).toBeTruthy();
  });
});
